import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://tuo-username.github.io',  // opzionale, ma utile per sitemap e link assoluti
  base: '/TestSiti/',  // aggiungi questo se il sito sarà su https://tuo-username.github.io/nome-repo/
});